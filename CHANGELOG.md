# Portfolio Changelog

Rolling 7-day log of changes to this portfolio. Entries older than 7 days are pruned — full history lives in `git log`.

**How to revert:** ask Claude "show me my portfolio history" or "revert to [date/commit]". Claude will offer a staging preview on the `staging` branch (Netlify deploys it to a separate URL) before reverting `main` with `git revert`.

---

## 2026-05-19

- **ad0aa0a** — Cisco IoT card: on hover, the static Cisco logo crossfades into an interactive Spline 3D robot arm scene (cursor-tracking). Viewer scaled 1.5× to fill the card; uses remote scene `gn6VgyuuEod0UuzZ` via `@splinetool/viewer@1.12.94`. Promoted from `staging` branch.

## 2026-05-18

- **dee3c32** — Added project category badges to Cloud Theory AI (AI, B2B, SaaS) and FloQast Reporting (Fintech, B2B, SaaS, 0→1). Replaced "Product Design" and "Enterprise/Reporting" tags.

---

_Entries older than 2026-05-12 have been pruned. Run `git log --since="14 days ago"` for older history._
